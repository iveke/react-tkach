export const getBalance = (state) => state.account.balance;

export const getList = (state) => state.todos;
export const getTitle = (state) => state.form.title;
export const getLevel = (state) => state.form.level;
export const getTheme = (state) => state.theme.color;